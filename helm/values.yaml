# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  #registry: "ghcr.io"
  repository: dennyyyyyyy
  #pullPolicy: IfNotPresent
  tag: git_hook-9826ff4
  # tag: v1.0.7-aebbbe9
  #  tag: v1.0.1-1-g370829a
  os: linux
  arch: amd64
secret:
  name: "kbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
